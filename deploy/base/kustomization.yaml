apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: solra-backend-secret
    envs:
      - /home/ubuntu/env/solra-backend-secret.env
    type: Opaque